import { createFileRoute, Link } from "@tanstack/react-router";
import { MapPin, Navigation, Search } from "lucide-react";
import { cinemas } from "@/data/cinebook";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export const Route = createFileRoute("/cinemas")({ head: () => ({ meta: [{ title: "Cinemas Near You — CineBook" }, { name: "description", content: "Explore premium cinemas, screens and movie showtimes near Mumbai." }, { property: "og:title", content: "Cinemas Near You — CineBook" }, { property: "og:description", content: "Explore premium cinemas and movie showtimes near you." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: CinemasPage });
function CinemasPage() { return <div className="page-shell pb-24 pt-32"><span className="eyebrow">Mumbai</span><h1 className="mt-3 font-display text-4xl font-bold sm:text-6xl">Find your perfect screen.</h1><div className="mt-8 flex max-w-2xl items-center gap-3"><div className="relative flex-1"><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground"/><Input placeholder="Search cinemas or neighbourhoods" className="h-12 bg-card pl-12"/></div><Button size="icon" variant="outline" aria-label="Use current location"><Navigation/></Button></div><div className="mt-12 space-y-5">{cinemas.map((cinema) => <article key={cinema.id} className="grid overflow-hidden rounded-lg border border-border bg-card md:grid-cols-[300px_1fr]"><img src={cinema.image} alt={`${cinema.name} cinema`} width={1536} height={1024} className="h-full min-h-56 w-full object-cover"/><div className="p-6 sm:p-8"><div className="grid grid-cols-[minmax(0,1fr)_auto] gap-4"><div><h2 className="font-display text-2xl font-bold">{cinema.name}</h2><p className="mt-2 flex items-center gap-2 text-sm text-muted-foreground"><MapPin className="size-4"/>{cinema.location}</p></div><span className="text-sm text-muted-foreground">{cinema.distance}</span></div><div className="mt-6 flex flex-wrap gap-2">{cinema.amenities.map((amenity) => <span key={amenity} className="rounded-md bg-secondary px-3 py-1.5 text-xs font-semibold">{amenity}</span>)}</div><div className="mt-7 flex gap-2"><Button asChild><Link to="/movies/$movieId" params={{ movieId: "echoes-of-arrakis" }}>View shows</Link></Button><Button variant="outline">Directions</Button></div></div></article>)}</div></div>; }