import { createFileRoute, Link } from "@tanstack/react-router";
import { CalendarDays, MapPin, Ticket } from "lucide-react";
import { useState } from "react";
import { movies } from "@/data/cinebook";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/bookings")({ head: () => ({ meta: [{ title: "My Bookings — CineBook" }, { name: "description", content: "View upcoming, completed and cancelled CineBook reservations." }, { property: "og:title", content: "My Bookings — CineBook" }, { property: "og:description", content: "Your movie tickets and booking history." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: BookingsPage });
function BookingsPage() { const [tab,setTab]=useState("Upcoming"); const movie=movies[0]; return <div className="page-shell min-h-[75vh] pb-24 pt-32"><h1 className="font-display text-4xl font-bold sm:text-5xl">My Bookings</h1><div className="mt-8 flex gap-2">{["Upcoming","Completed","Cancelled"].map((item)=><Button key={item} variant={tab===item?"default":"secondary"} onClick={()=>setTab(item)}>{item}</Button>)}</div>{tab==="Upcoming"?<article className="mt-10 grid max-w-3xl overflow-hidden rounded-lg border border-border bg-card sm:grid-cols-[180px_1fr]"><img src={movie.poster} alt="Echoes of Arrakis poster" width={1024} height={1536} className="h-full max-h-80 w-full object-cover"/><div className="p-6"><span className="eyebrow">Booking confirmed</span><h2 className="mt-3 font-display text-2xl font-bold">{movie.title}</h2><div className="mt-5 space-y-3 text-sm text-muted-foreground"><p className="flex items-center gap-2"><CalendarDays className="size-4"/>Saturday, 20 September · 7:30 PM</p><p className="flex items-center gap-2"><MapPin className="size-4"/>Aurora Cinemas · Phoenix Palladium</p><p className="flex items-center gap-2"><Ticket className="size-4"/>Seats A10, A11</p></div><div className="mt-7 flex gap-3"><Button asChild><Link to="/confirmation">View ticket</Link></Button><Button variant="outline">Cancel booking</Button></div></div></article>:<div className="mt-16 max-w-lg"><Ticket className="size-10 text-primary"/><h2 className="mt-5 font-display text-2xl font-bold">Your movie journey starts here 🍿</h2><p className="mt-2 text-muted-foreground">No {tab.toLowerCase()} bookings yet.</p><Button asChild className="mt-6"><Link to="/movies">Explore movies</Link></Button></div>}</div>; }