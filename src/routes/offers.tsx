import { createFileRoute } from "@tanstack/react-router";
import { Copy, Sparkles } from "lucide-react";
import { toast } from "sonner";
import { offers } from "@/data/cinebook";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/offers")({ head: () => ({ meta: [{ title: "Movie Offers — CineBook" }, { name: "description", content: "Save on movie tickets, food combos and premium cinema experiences." }, { property: "og:title", content: "Exclusive Movie Offers — CineBook" }, { property: "og:description", content: "Unlock savings on cinema tickets and movie experiences." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: OffersPage });
function OffersPage() { return <div className="page-shell pb-24 pt-32"><Sparkles className="size-7 text-primary"/><h1 className="mt-4 font-display text-4xl font-bold sm:text-6xl">Good stories. Better prices.</h1><p className="mt-4 max-w-xl text-muted-foreground">Handpicked ticket, bank and food offers for your next cinema visit.</p><div className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{[...offers, ...offers].map((offer,index) => <article key={`${offer.code}-${index}`} className={`${offer.tone} min-h-72 rounded-lg p-7 shadow-card`}><p className="text-xs font-bold uppercase text-foreground/70">{index > 2 ? "MEMBER EXCLUSIVE" : offer.kicker}</p><h2 className="mt-8 max-w-[15ch] font-display text-3xl font-bold">{offer.title}</h2><p className="mt-4 text-sm text-foreground/70">Valid on selected cinemas and showtimes. Terms apply.</p><div className="mt-9 flex items-center justify-between border-t border-foreground/20 pt-5"><code className="text-lg font-bold">{offer.code}</code><Button variant="secondary" onClick={() => { navigator.clipboard?.writeText(offer.code); toast.success("Offer code copied"); }}><Copy/> Copy</Button></div></article>)}</div></div>; }